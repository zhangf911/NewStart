#这里用来记录一个网络游戏库应该需要哪些基本工具，当然有些是通用的，并不局限于网络游戏底层库

现列举如下：
1、日志库（输出重定向，控制台，文件，数据库）
2、线程库（包括定时器，线程同步对象，线程池，原子对象）
3、日期工具（各种时间换算，日期记录）
4、随机数生成器
5、网络库（异步服务器，异步客户端，序列化（大小端转换），微型http服务器，RPC）
6、文件工具
7、控制台GUI
8、设计模式（单件，工厂，观察者）

